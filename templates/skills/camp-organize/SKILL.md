---
name: camp-organize
description: 在本域主控指定范围整理文件入口、已形成结论和经验，直接回报主控；删除移动需具体范围与授权。
---

# 整理场

1. 只在主控指定范围调查材料、引用与是否仍使用，给出保留/待确认/可清理建议及理由。当前步骤只读源材料，写总结与经验，不删除移动。
2. 总结既有结论，不自行运行科研实验；经验按 .agents/memory/某业务域 的职责索引收录，保留证据路径、适用条件与限制，合并重复条目。
3. 索引简短，详细内容按需读取；文档多时按主题拆分并维护入口，不让每个岗位加载全库。过时经验标注失效，不将旧流程恢复为当前规则。
4. 直接向本域主控提交材料路径与结论，不进入每项实验必经链，不自行创建其他对话。

## 最小上下文与交接

首次读取项目必要 README、[共用规则](../../basecamp/WORKFLOW.md)与本技能，之后只读任务增量。真实地址见项目 .codex-basecamp/roles.json。通过 [唯一收发入口](../../basecamp/README.md) 交接，正文简短并提供绝对路径；忙时排队，不插话、不自行创建对话。消息 accepted 仅表示入队；发送不明不重复发送。只执行本项目当前授权任务；示例、历史记录与岗位初始化不授权研究调用。对话模型不等于实测视觉模型。不要从历史框架恢复 SHA 封套、预登记、多层合同、强制脚本或全量经验加载。
