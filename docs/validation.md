# 首版验证记录

日期：2026-09-22，Windows；Python标准库测试，无外部视觉/业务供应商调用。

## 已执行

- `python -m unittest discover -s tests -q`：39项，38通过，1项跳过。跳过项是实际创建符号链接后的路径逃逸检查，本机不允许创建该测试链接；代码中保留链接与junction路径拒绝逻辑。
- 安装测试覆盖空目录、中文/空格路径、已有README/AGENTS/业务代码保护、冲突前置检查、dry-run不落文件、同版重复安装、已编辑规则不覆盖、非法域名称拒绝。
- 真正通过安装后的独立运行器在第二个临时项目再次展开，子进程仅使用已安装代码与模板，不引用来源项目或源框架目录。
- 模拟岗位创建覆盖精确项目目录绑定、明确共享目录、重复执行零新增、创建回复丢失保留不明、pending客户ID不冒充真实ID、绑定后继续、重复角色/跨项目绑定拒绝、明确模型覆盖与继承分离。
- 继承的收发室/桌面测试覆盖接收方独立阻塞、启动前失败、入队后连接关闭、冷激活重试不重发、忙碌不插话、页面返回所属域主控、Windows锁竞争、安装包更新与初始化恢复上限。
- 真实`doctor --live`：CLI 0.154.0版本查询、App Server初始化、实验接口模式中的thread/queue/add、当前桌面create_thread/list_projects/wait_threads/navigate_to_codex_page能力全部通过。创建0、发送0、业务调用0。
- 真实只读接口核对确认wait_threads不提供cwd且不能等待调用者自身；绑定已改为thread/read验证目录与身份，status跳过等待当前调用者。

## 边界

没有在本次新建真实岗位、测试付费模型、启动研究或给来源项目岗位发送消息。因此以上证明文件可展开、模拟通信边界与当前能力可用，不证明真实多岗位完整运行、所有权限/速度设置或未来桌面版本兼容。

每个新项目首次展开仍应由其主控在当前授权内观察真实岗位初始化和首次交接；缺能力时报告具体缺口。

## 下载与隐私复验

发布前重新运行全部离线测试，并在独立目录检查文件展开、重复安装和安装副本的能力检查。公开版本使用独立的干净Git历史；发布检查覆盖全部公开文件、提交元数据与历史对象。内部来源信息及私人讨论不在公开包中。

GitHub下载副本的复验结果在发布完成后记录于此。
