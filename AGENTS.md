# Codex Basecamp

本仓库是一套可展开到其他项目的多对话协作框架。先读 README.md 和 START.md。

- 用户要求“展开／搭建／使用基地车”时，按 START.md 执行初始化、能力检查和岗位登记；文件初始化不等于对话已创建。
- 用户要求维护框架时，改 framework/、templates/ 和相关 tests/；不替用户创建当前不需要的岗位。
- 保留主控→规划→试验→分析→主控及直属整理岗；共享项目目录必须是用户选择。
- 不导入来源项目的任务、对话ID、认证、个人路径或运行状态。
- 新建岗位必须对应用户明确请求；权限与模型继承用户设置，配置期望不冒充验证结果。
- 验证：`python -m unittest discover -s tests -v`；离线测试不启动真实模型或现有项目岗位。
- 模板更新后，用临时空项目和已有AGENTS的项目检查展开、重复执行及冲突处理。
